#include <stdio.h>
int main()
{
    // Las variables utilizadas son i para el ciclo For (para el control del ciclo) y la variable mul que es la variable que saca el resultado de la multiplicación 
    int i,mul;
    printf("-------------Bienvenido--------- \n");
    printf("Los primeros 20 multiplos de 3 son:\n");
    // Ciclo For en el cual se utiliza se multiplica el numero tres por la variable i en cada iteracion. El resultado de esto da la variable multiplicacion.
    for(i=1; i<= 20; i++){
    mul=3*i;
    printf("3 x %d = %d \n", i,sum);
    }
    return 0;
}
